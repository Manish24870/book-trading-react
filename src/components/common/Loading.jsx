import "./Loading.css";

const Loading = (props) => {
  return (
    <div style={{ textAlign: "center", marginTop: 20 }}>
      <span className="loader"></span>
    </div>
  );
};

export default Loading;
